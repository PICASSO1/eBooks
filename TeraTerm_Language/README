
TeraTerm Language Official WebSite: https://teratermproject.github.io/manual/5/en/macro/syntax/index.html

/***********************************************************************************************************/

TeraTerm Language (TTL) 僅支援４種資料型態：

1. 整數 (Integer)：32-bit 的整數，範圍為 -2,147,483,648 到 2,147,483,647；不支援浮點數 (Floating)。

2. 字串 (String)：包含 NULL 以外的任意字元；字串的最大長度為 511。

3. 整數陣列 (Integer Array)：可以使用 intdim 巨集指令來建立整數陣列；最大的索引值為 65,536。

4. 字串陣列 (String Array)：可以使用 strdim 巨集指令來建立整數陣列；最大的索引值為 65,536。

/***********************************************************************************************************/

Formats of Constants (常數格式)：

1. 整數常數：整數常數表示為以 "$" 字元開頭的１０進位數 (Decimal)或１６進位數 (Hexadecimal )。不支援浮點數運算。
   EX: 123 , -11 , $3a (0x3A), $10F (0x010F)

2. 字串常數：用 單引號 (')或是 雙引號 (")括起來的字串。
   EX: "Hello!! " , 'World!! '

3. 單一字符：以 # 再加上 ASCII 碼；Dec 或 Hex 均可。
   EX: #65 (Dec)為 'A' ；#$41 (Hex)亦為 'A' ；#13 為 CR (Carriage Return)；#10 為 LF (Line Feed)。

4. 負數：
   `for i 5 -1` 這種表示法會有問題：
   第二參數很容易被解讀為 (5 - 1) 而並不是 5
   解決方案：
   a). -1 前面加上 0： 0-1 (也是 -1；但並不直覺)
   b). 負數以括號表示： `for i 5 (-1)`
   c). 將負數設定為一個變數： A = -1 `for i 5 A`

/***********************************************************************************************************/

Identifiers and Reserved Words (識別字和保留字)：

1. 變數 (Variable)的第一個字元必須是字母或是底線 ('_')；變數不區分大小寫，最大長度為３１個字元。

2. 標籤 (Label)識別字可以為字母、數字和底線 ('_')，最大長度為３１個字元。

3. 保留字 (Reserved Words)的規則和一般的程式語言一樣。

/***********************************************************************************************************/

Line Formats (行格式):

1. 註解 以 ';' 的行開頭，或是 C語言的 /* .... */；但不支援 "//" 開頭的註解。

2. TTL 的一行最多只支援到 1,023 個字元，多出來的部份會被忽略。

3. 標籤行的命名方式為 冒號 (':') 開頭。

4. 參數 (Parameter)的傳遞：TTL 並沒有支援函式 (Function)；這裡指的是一般的 TTL 指令：
   EX: 
   ttlcommand 10 20 
   ttlcommand "width=10" "height=20" 
   ttlcommand 10 20 "width=30" "height=40" 
